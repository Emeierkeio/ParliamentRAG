# Dependencies for the KG construction pipeline (build/), pinned for
# reproducibility — versions verified working on 2026-07-24.
# Installed on top of backend/requirements.txt (neo4j, python-dotenv, ...).
# Kept in sync with build/pyproject.toml.
pandas==3.0.5
regex==2026.7.19
spacy==3.8.14
openai==2.46.0
tqdm==4.68.4
# Senate downloads: senato.it is behind an AWS WAF JS challenge.
# After install, run once: playwright install chromium
playwright==1.61.0

# RDF export (export_rdf.py)
rdflib==7.6.0
